
TODO

SHORT TERM

-> Easier way to write exclusion constraints, active and activetype
- parameters, such as tc.SOC_TYPE, tc.SOC_INITIAL, tc.SOC_BACKGROUND, local to tasks, numbered (MEXEC TASKNET HAS THIS)

MID TERM

- Task decompositions, Compositional reasoning (methodology?)
- Check for non-existence of plan in any initial state (negation like for properties)
- Change documentation look
- Check again effects of soft constraints (priorities, start, end, optional)
- Research MEXEC documentation

LONG TERM

- VS code extension
- Allow option to only run safety check (they can be faster)
- Python API to the tool.
- Modules
- right-after or give time constraints
- Non-deterministic rates
- Translator to Python
- tlcons not allowed in temporal formulas (not clean)

MEXEC:

- Types
- Timeline and task ids are used for reference
- MEXEC uses completion timelines for chaining (not sure what it means)
- TILs: environment updates to timelines (not sure what it means)
- Most V&V efforts focus on the execution. where tasks can be re-arranged.

MINOR:

- Consider preferred_duration (instead of duration) as keyword, same with start
- rename optimize to schedule and satisfy to verify
- rate keyword causes confusion

DONE SINCE LAST MMR:

- Improved output on website for usat
- Added delete all report buttons
- fixed XML translation of preferred duration
- Understand the zone concept better, fixed rate updates
- atomic: B or N (MEXEC distinguishes between atomic and Boolean timelines.) mexec/xml: pre-condition 0 is translated to false
- min/max instances of optional tasks (MEXEC TASKNET HAS THIS)