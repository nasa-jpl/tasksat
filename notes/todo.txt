
TODO

SHORT TERM

- Unsat core
- Colors for results
- Store results in a JSON file
- rate timelines can have an initial rate (MEXEC TASKNET HAS THIS)
- two kinds of rate impacts: CUMULATIVE_RATE (value is added) and ASSIGNMENT_RATE (value is assigned)
- MEXEC allows maint updates for atomic timelines
- // comments
- Priority: order in which it tries to schedule
  Bigger number means higher priority
- Requests: maximal
- Consider preferred_duration (instead of duration) as keyword, same with stat, end
- rate keyword causes confusion
- Pre{ ... cannot be empty ...} etc.
- Shorthand for overlap constraints
- Check again effects of soft constraints (priorities, start, end, optional)
- Visualizing a schedule (e.g. Gant chart)
- other syntax: implies
- true and false as temporal formulas
- tlcons not allowed in temporal formulas (not clean)
- static check does not check duplicates of constraint names
- rename optimize to schedule and satisfy to verify

BUGS

- check 'eventually active(drive) in example for paper, Some problem with constraints at some point (maybe it got fixed).
-> Remove examples that do not compile

MID TERM

- min/max instances of optional tasks (MEXEC TASKNET HAS THIS)
- Allow option to only run safety check (they can be faster)
- Include temporal properties as constraint in scheduling phase
- Python API to the tool.
- Visualizing a schedule (e.g. Gant chart)
- Modules
- right-after or give time constraints

LONG TERM

- One could start with PDDL 3 - if we want to go down the road of an external DSL.
- Most V&V efforts focus on the execution. where tasks can be re-arranged.
- Maybe one exactly wants to know what MEXEC does.
- Scalability
- Non-deterministic rates

MEXEC:

- Tyoes
- Timeline and task ids are used for reference
- Instances might not even be scheduled.
- MEXEC uses completion timelines for chaining
- parameters, such as tc.SOC_TYPE, tc.SOC_INITIAL, tc.SOC_BACKGROUND, local to tasks, numbered (MEXEC TASKNET HAS THIS)
- They distinguish between atomic and Boolean timelines.
- TILs: environment updates to timelines.